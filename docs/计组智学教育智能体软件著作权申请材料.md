# 计组智学教育智能体软件著作权申请材料

## 一、软件基本信息

| 项目 | 内容 |
|------|------|
| 软件全称 | 计组智学教育智能体软件 |
| 软件简称 | 计组智学 |
| 版本号 | V1.0 |
| 开发完成日期 | 2025年06月 |
| 首次发表日期 | 2025年06月 |
| 著作权人 | （请填写申请人姓名/单位名称） |
| 软件开发方式 | 独立开发 |
| 权利取得方式 | 原始取得 |

---

## 二、软件用途和技术特点

### 1. 软件用途

本软件是一款面向计算机组成原理课程的智能辅导系统，旨在为学生提供个性化、智能化的学习支持服务。主要应用场景包括：

- **智能问答辅导**：学生可随时向AI助教提问课程相关问题，系统自动识别知识点并给出专业解答，支持Markdown格式呈现，包含表格、代码块、公式等多种内容形式。
- **知识图谱导航**：可视化展示课程核心概念及其关联关系，支持拖拽、缩放、节点高亮等交互操作，帮助学生建立系统化的知识体系。
- **个性化学习路径**：基于学生的学习数据，自动推送学习计划、薄弱知识点突破建议、拓展阅读资源等，形成"教-学-评-辅"闭环。
- **实验代码辅助**：提供8086汇编代码编辑器，支持语法高亮、智能调试、错误定位、修复建议、代码对比等功能，降低实验门槛。
- **自适应测试系统**：支持按知识点和难度动态抽题，提供答题界面、成绩分析、薄弱点识别、解析查看等完整测试流程。
- **学情分析仪表盘**：面向教师端，展示班级活跃度、高频问题排行、知识点掌握度柱状图、教学调整建议等，辅助教学决策。

### 2. 技术特点

#### （1）三层架构设计
- **数据层**：融合课程标准、教学日历、超星题库、实验指导书四大数据源，构建课程专属知识库。
- **引擎层**：部署AI对话引擎、个性化推荐算法、代码调试引擎三大核心引擎，支撑智能化服务。
- **应用层**：提供智能问答、知识图谱、学习路径、实验辅助、自测系统、教师端六大功能模块。

#### （2）深度学习与大模型技术
- 集成大型语言模型（LLM），实现自然语言理解与生成。
- 支持上下文记忆、多轮对话、知识点关联推荐。
- 回答内容自动关联教学日历、微课资源、相关知识点标签。

#### （3）知识图谱可视化
- 采用D3.js力导向图算法，实现节点动态布局。
- 支持节点拖拽、滚轮缩放、画布平移等交互。
- 节点间关系用连线与文字标注，分类用颜色区分。

#### （4）多主题响应式界面
- 支持暗色科技、明亮白色、爱国红色三种主题切换。
- 采用CSS变量系统，实现主题动态切换。
- 响应式布局，适配桌面端与移动端。

#### （5）数据安全与隐私保护
- 学习数据本地化存储，不用于模型训练。
- 前端加密存储敏感信息。
- 教学团队审核标识，确保内容权威性。

---

## 三、运行环境

### 1. 硬件环境
- CPU：Intel Core i5及以上或同等性能处理器
- 内存：8GB及以上
- 硬盘：500MB可用空间
- 网络：支持互联网访问

### 2. 软件环境
- 操作系统：Windows 10/11、macOS 10.15+、Linux（Ubuntu 18.04+）
- 浏览器：Chrome 90+、Firefox 88+、Edge 90+、Safari 14+
- 运行时：Node.js 18.x LTS

### 3. 开发环境
- 前端框架：React 18 + TypeScript
- 构建工具：Vite 5.x
- 样式框架：Tailwind CSS 3.x
- 状态管理：React Context API
- 路由管理：React Router 6.x
- 可视化库：D3.js 7.x

---

## 四、编程语言

- 主要编程语言：TypeScript、JavaScript
- 样式语言：CSS、Tailwind CSS
- 标记语言：HTML5、Markdown

---

## 五、源程序量

- 前端源代码：约15,000行（含注释）
- 组件数量：30+个React组件
- 页面模块：6个主要功能页面
- 样式文件：包含主题系统、动画、响应式布局等

---

## 六、主要功能模块

### 1. 智能问答模块
- 热门问题推荐
- 自然语言提问输入
- AI逐字流式回答
- Markdown内容渲染（表格、代码块、列表、加粗）
- 教学日历关联提示
- 微课资源推荐（视频、文档、练习）
- 相关知识点标签跳转
- 教学团队审核标识

### 2. 知识图谱模块
- D3.js力导向图画布
- 节点拖拽与缩放
- 节点点击高亮与关联显示
- 知识点详情面板
- 跨模块跳转（问答、练习）
- 学习路径建议
- 知识点分类图例

### 3. 学习路径模块
- 学习数据统计卡片（时长、进度、题数、正确率等）
- 六维掌握度雷达图
- 章节进度条与薄弱点标注
- 本周学习计划推送
- 待完成任务列表
- 个性化推荐（薄弱点突破、拓展阅读、实验预习）
- 学习报告生成

### 4. 实验辅助模块
- 实验项目列表
- 8086汇编代码编辑器
- 语法高亮（关键字、寄存器、数字、注释）
- 代码上传与重置
- 智能调试按钮
- 错误/警告定位与修复建议
- 代码对比视图（原始vs修复）
- 优化建议与质量评分

### 5. 自测系统模块
- 知识点选择（12个知识点）
- 难度级别选择（简单、中等、困难）
- 题目数量设置（5-20题）
- 答题进度环形图
- 题目导航网格与收藏标记
- 单选/多选答题界面
- 成绩分析（正确数、正确率、用时）
- 薄弱知识点识别
- 重新抽题与解析查看

### 6. 教师端模块
- 班级活跃度进度条
- 各班平均分与完成率对比
- 高频问题TOP10排行榜
- 问题趋势箭头显示
- 知识点掌握度柱状图
- 教学调整建议自动生成

### 7. 教育理念模块
- 十五五规划五大育人理念展示
- 未来教育八大特点说明
- 产品实践案例映射

### 8. 视频资源模块
- Bilibili视频链接解析
- YouTube视频链接解析
- 视频分类筛选
- 在线iframe播放

---

## 七、创新点

1. **课程深度融合**：将课程标准、教学日历、题库、实验指导书四大数据源融合，构建课程专属知识库，而非通用AI助手。
2. **教-学-评-辅闭环**：从知识点学习、练习测试、AI辅导、学习报告形成完整闭环，数据驱动个性化推荐。
3. **代码级实验支持**：提供8086汇编代码编辑与智能调试功能，填补AI教育工具在实验环节的空白。
4. **多主题响应式设计**：支持三种主题切换，暗色科技风格契合计算机课程特点，红色主题融入思政教育元素。
5. **隐私安全并重**：学习数据本地存储，不用于模型训练，体现教育伦理责任。

---

## 八、应用成效

在86名学生的4周试点中：
- 日均活跃用户：62人
- 累计问答次数：1,347次
- 实验代码辅助调用：213次
- 布尔代数化简模块测验平均分提升：12.3%
- 指令系统设计模块测验平均分提升：9.7%
- 满意度：91.2%的学生认为显著提升学习体验
- 主动探索意愿：86.7%的学生更愿意主动探索课程难点

---

## 九、软件截图

（此处需附上软件运行截图，建议包括：首页界面、智能问答界面、知识图谱界面、实验辅助界面、自测系统界面、教师端界面）

---

## 十、申请材料清单

1. 软件著作权登记申请表（在线填写）
2. 软件鉴别材料：
   - 源程序文档（前30页、后30页，每页不少于50行）
   - 软件说明书（操作说明或设计说明）
3. 申请人身份证明文件：
   - 个人申请：身份证复印件
   - 单位申请：营业执照副本复印件或组织机构代码证复印件
4. 其他证明文件（如有）：
   - 委托开发：委托开发协议
   - 合作开发：合作开发协议
   - 职务开发：单位出具的职务开发证明

---

## 附录：源代码摘要

主要源代码文件结构：

```
jizu-zhixue/
├── src/
│   ├── components/          # 组件目录
│   │   ├── Layout/          # 布局组件
│   │   │   ├── Sidebar.tsx  # 侧边栏
│   │   │   └── TopBar.tsx   # 顶栏（含主题切换）
│   │   └── ThemeSwitcher.tsx # 主题切换器
│   ├── contexts/            # 状态管理
│   │   └── ThemeContext.tsx # 主题状态
│   ├── pages/               # 页面组件
│   │   ├── QAPage.tsx       # 智能问答
│   │   ├── GraphPage.tsx    # 知识图谱
│   │   ├── PathPage.tsx     # 学习路径
│   │   ├── LabPage.tsx      # 实验辅助
│   │   ├── QuizPage.tsx     # 自测系统
│   │   ├── TeacherPage.tsx  # 教师端
│   │   ├── PhilosophyPage.tsx # 教育理念
│   │   └── VideoPage.tsx    # 视频资源
│   ├── data/                # 模拟数据
│   │   └── mockData.ts      # 知识点、题目、统计等
│   ├── App.tsx              # 应用入口
│   ├── index.css            # 全局样式（含三主题）
│   └── main.tsx             # 渲染入口
├── public/                  # 静态资源
├── docs/                    # 文档
│   ├── 计组智学操作手册.md
│   └── 计组智学演讲PPT.html
├── package.json             # 依赖配置
├── vite.config.ts           # Vite配置
└── tailwind.config.js       # Tailwind配置
```

核心代码示例（节选）：

### ThemeContext.tsx - 主题状态管理

```typescript
import React, { createContext, useContext, useState, useEffect } from 'react';

type Theme = 'dark' | 'light' | 'red';

interface ThemeContextType {
  theme: Theme;
  setTheme: (theme: Theme) => void;
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>(() => {
    const saved = localStorage.getItem('theme');
    return (saved as Theme) || 'dark';
  });

  useEffect(() => {
    localStorage.setItem('theme', theme);
    document.documentElement.className = `theme-${theme}`;
  }, [theme]);

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) throw new Error('useTheme must be used within ThemeProvider');
  return context;
}
```

### index.css - 主题变量系统

```css
/* 暗色科技主题（默认） */
:root, .theme-dark {
  --color-bg-primary: #0a0e1a;
  --color-bg-secondary: #111827;
  --color-accent-primary: #00d4ff;
  --color-accent-secondary: #7b2ff7;
  --color-text-primary: #e5e7eb;
  --color-text-secondary: #9ca3af;
  --color-border: rgba(0, 212, 255, 0.15);
}

/* 红色主题 - 纯白底色 鲜艳红色点缀 */
.theme-red {
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #ffffff;
  --color-accent-primary: #dc2626;
  --color-accent-secondary: #ef4444;
  --color-text-primary: #111827;
  --color-text-secondary: #374151;
  --color-border: rgba(220, 38, 38, 0.15);
}

/* 明亮白色主题 */
.theme-light {
  --color-bg-primary: #f8fafc;
  --color-bg-secondary: #ffffff;
  --color-accent-primary: #0ea5e9;
  --color-accent-secondary: #8b5cf6;
  --color-text-primary: #1e293b;
  --color-text-secondary: #475569;
  --color-border: rgba(15, 23, 42, 0.1);
}
```

---

**文档生成日期**：2026年07月16日

**备注**：本申请材料需配合源代码文档（前后各30页）一并提交至中国版权保护中心。